'use client'

import React from 'react';
// This component is disabled as we no longer use Sanity CMS
// Keeping for potential future use

export default function BlogContent() {
  return (
    <div className="prose prose-lg max-w-none">
      <p>Blog content rendering is currently disabled as we moved to Ghost CMS.</p>
    </div>
  );
}